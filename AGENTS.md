# HomeIQ Invest

This repository is platform-independent. Use standard npm scripts for local development and deployment.

- `npm install`
- `npm run dev`
- `npm run build`

Keep secrets in local or hosting environment variables. Never commit service-role keys or AI API keys.
